export type ElementosDados = {
  idelementos?: number;
  descricao_elementos?: string;
};

export type InfoElementos = {
  elementos?: {
    elemento?: object[];
  };
};

// ctrl + alt + f = retira aspas
